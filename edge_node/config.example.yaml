edge_node:
  id: 1
  name: "ru-msk-01"
  location: "RU-MSK"
  
control_plane:
  url: "https://control.yourcdn.ru"
  api_key: "your-api-key-here"
  
update_interval: 30  # seconds, main config/purge loop tick
request_timeout: 15.0  # seconds for HTTP requests to control plane
heartbeat_interval: 30  # seconds, runs in its own task independent of update_interval
heartbeat_timeout: 5.0  # short timeout so a slow control plane doesn't delay heartbeats

nginx:
  config_path: "/etc/nginx/conf.d/cdn.conf"
  certs_dir: "/etc/nginx/ssl/cdn"
  reload_command: "systemctl reload nginx"
  test_command: "nginx -t"
  
monitoring:
  enabled: true
  endpoint: "https://control.yourcdn.ru/api/v1/edge/metrics"

